	
	ORG	00H
	
	MOV	SP, #5FH
	MOV	PSW,#00H
OVER:	MOV     A,  #00000001B
	MOV     R0, #7
	MOV     R5, #7

AGAIN:	MOV	P2,A
	LCALL	DELAY
	RL      A
	DJNZ	R0,AGAIN
	MOV     A,  #10000000B

AGAIN_:	MOV	P2,A
	LCALL	DELAY
	RR      A
	DJNZ	R5,AGAIN_
	SJMP    OVER   
	
	
	
	
DELAY:	MOV	R2, #10
L3:	MOV	R3, #210
L2:	MOV	R4, #200
L1:	DJNZ	R4, L1
	DJNZ	R3, L2
	DJNZ	R2, L3
	RET
	
	END